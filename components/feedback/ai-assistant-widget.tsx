"use client";

/**
 * Admin no longer ships canned “demo” disease answers. The live Health Data
 * Assistant lives on the public portal (nsgdp-frontend). This stub avoids
 * accidental re-mount of fake Q&A if the widget is imported again.
 */
export function AiAssistantWidget() {
  return null;
}
